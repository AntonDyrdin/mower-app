{
  "name": "mower-app",
  "version": "1.0.0",
  "description": "Android UI для робота-газонокосилки",
  "scripts": {
    "build": "xcopy /E /I /Y src dist",
    "sync": "npm run build && npx cap sync android",
    "open": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/assets": "^3.0.5",
    "@capacitor/cli": "^8.4.1"
  }
}
